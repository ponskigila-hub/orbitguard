# OGB Frontend — environment variables
# Copy this file to .env.local (already git-ignored) and fill in the values.

# URL of the running OGB FastAPI backend.
# Local dev:   http://localhost:8000  (default — no change needed for local run)
# Production:  set to your deployed backend URL if you deploy the backend
NEXT_PUBLIC_API_URL=http://localhost:8000

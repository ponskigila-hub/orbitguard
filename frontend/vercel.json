{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "Public URL of the OGB FastAPI backend (e.g. https://ogb-backend.onrender.com). Set this in the Vercel project settings.",
      "required": true
    }
  }
}
